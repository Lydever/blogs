/**
 * @Description: Proxy和Reflect之略知
 * @Author:   liyingxia
 * @CreateDate:  2022/12/15 10:25
 */
// 1)、Reflect 设计目的
//
// Reflect对象与Proxy对象一样，都是 ES6 为了操作对象而提供的新 API。Reflect对象的设计目的有这样几个。
// （1）将Object对象的一些明显属于语言内部的方法（比如Object.defineProperty），放到Reflect对象上。也就是说，从Reflect对象上可以拿到语言内部的方法。1)、Reflect 设计目的
// Reflect对象与Proxy对象一样，都是 ES6 为了操作对象而提供的新 API。Reflect对象的设计目的有这样几个。
// （1）将Object对象的一些明显属于语言内部的方法（比如Object.defineProperty），放到Reflect对象上。也就是说，从Reflect对象上可以拿到语言内部的方法。

